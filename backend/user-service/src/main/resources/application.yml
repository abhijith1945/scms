server:
  port: 8081

spring:
  application:
    name: user-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/scms_users
    username: postgres
    password: scms_pass_2025
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: scms_super_secret_jwt_key_2025_college
  expiration: 86400000

file:
  upload-dir: ./uploads/assignments

logging:
  level:
    com.scms: DEBUG
    org.springframework.security: DEBUG
